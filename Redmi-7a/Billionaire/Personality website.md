### Ui improvement 

1. Decrease Image width slightly 
2. I am gonna put activities and wise words on same html so explore themes for remeber this, wise words sections
3. Fix that fucking repeating images
4. place all css in files instead of style tag
5. 

### Add new content

- Use `<span class="blue>` to highlight Good points
- Use `<span class="red>` to highlight bad points in paragraph 
- use `<ul class="avoid">` for bad bullet points
